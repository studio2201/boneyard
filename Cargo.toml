[package]
name = "boneyard"
version = "0.2.1"
edition = "2021"
publish = false
description = "Org-wide tech-debt radar and repo scoring engine"
authors = ["studio2201"]
license = "Apache-2.0"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
panic = "abort"
